{
  "rewrites": [
    { "source": "/vault/:path*", "destination": "/vault/:path*" },
    { "source": "/api/:path*", "destination": "/vault/api/:path*" },
    { "source": "/:path*", "destination": "/vault/:path*" }
  ]
}
